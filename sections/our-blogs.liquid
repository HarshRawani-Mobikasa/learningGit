<section id="Our-Blog">
  <div class="container-fluid">
    <div class="section-title">
      <h2>Our Blogs</h2>
    </div>
    <div class="row">
      <div
        class="col-lg-12 d-flex flex-column justify-content-center pt-lg-0 order-2 order-lg-1"
        data-aos="fade-up"
        data-aos-delay="200"
      >
        <div class="blog-items">
          {% for block in section.blocks %}
            <div class="item">
              <img src="{{ block.settings.blog_img | img_url:'medium' }}">
              <span>{{ block.settings.heading }}</span>
              <p>{{ block.settings.heading2 | truncate: 15 }}</p>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
    "name": "Our Blog Section",

    "blocks": [
      {
        "type": "text",
        "name": "slide",
        "settings": [
        {
          "type": "image_picker",
          "id": "blog_img",
          "label": "Blog Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Date"
        },

            {
          "type": "text",
          "id": "heading2",
          "label": "Blog Text"
        }

        ]
      }
    ],
    "presets": [
      {
        "name": "Our Blog Section",
        "category": "text",
        "blocks": [
          {
            "type": "text"
          }
        ]
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
